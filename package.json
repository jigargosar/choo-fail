{
  "name": "choo-fail",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "rm -fr ./choo-test-cca && npx create-choo-app choo-test-cca 'Check if CCA app builds' && cd choo-test-cca && npm add nanologger",
    "posttest": "echo 'require(\'nanologger\') \n' >> index.js && npm run build"
  },
  "dependencies": {
   },
  "devDependencies": {
   }
}
